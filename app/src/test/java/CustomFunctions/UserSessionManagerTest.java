package CustomFunctions;

import org.junit.Test;

import static org.junit.Assert.*;

public class UserSessionManagerTest {

    @Test
    public void newTest() {
        //assertEquals("", UserSessionManager.getUserID());
        //assertEquals("", UserSessionManager.getUserToken());
        //UserSessionManager.Login("mahmoud_morsy@live.com", "Mahmoud123456789");
        //assertEquals(true, UserSessionManager.getUserID() != "");
        //assertEquals(false, UserSessionManager.getUserToken() != "");
    }
}